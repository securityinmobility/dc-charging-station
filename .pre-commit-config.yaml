repos:
  - repo: https://github.com/psf/black
    rev: 23.9.1  # Use the latest version or specify a compatible one
    hooks:
      - id: black
        #  - repo: https://github.com/pre-commit/mirrors-mypy
        #    rev: "v1.4.1"  # Use the latest stable mypy version
      #    hooks:
      #      - id: mypy
      #        args: ["--ignore-missing-imports", "--explicit-package-bases"]  # Optional: Adjust mypy arguments as needed

